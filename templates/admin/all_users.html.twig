{% extends 'base.html.twig' %}

{% block title %}Listes des profils{% endblock %}

{% block body %}
{% for user in users %}
    <div class="container">
        <div class="row justify-content bg-light rounded">
            <div class="col p-3">
                <h2>{{ user.nom }} {{ user.prenom }}</h2>
            </div>
            <div class="col mt-auto mb-auto">
                <a href="{{path('profile', {'id': user.id})}}"><button class="btn btn-success">Accéder au profil</button></a>
            </div>
        </div>
    </div>
    </br>
{% endfor %}
{% endblock %}